module.exports = {
  // @babel/preset-env -> responsável por alterar as func do JS que o navegador ainda não entende
  // @babel/preset-react -> transforma as coisas que o nav não entende do react (jsx)
  presets: ["@babel/preset-env", "@babel/preset-react"]
};
